{
  "name": "pos-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
